secrets:
  postgres_admin_password: {file: ./.secrets/postgres-admin-password}
  postgres_password: {file: ./.secrets/postgres-password}
  postgres_retention_password: {file: ./.secrets/postgres-retention-password}
  scoring_service_token: {file: ./.secrets/scoring-service-token}
  pii_encryption_key: {file: ./.secrets/pii-encryption-key}
  pii_hmac_key: {file: ./.secrets/pii-hmac-key}
  terms_runtime_password: {file: ./.secrets/terms-runtime-password}
  terms_migrator_password: {file: ./.secrets/terms-migrator-password}
  terms_retention_password: {file: ./.secrets/terms-retention-password}
  terms_backup_password: {file: ./.secrets/terms-backup-password}
  terms_internal_service_token: {file: ./.secrets/terms-internal-service-token}
  terms_fingerprint_key: {file: ./.secrets/terms-fingerprint-key}

services:
  frontend:
    networks: [backend,edge]

networks:
  edge: {}
