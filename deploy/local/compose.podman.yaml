secrets:
  postgres_admin_password:
    external: true
    name: postgres_admin_password
  postgres_password:
    external: true
    name: postgres_password
  postgres_retention_password:
    external: true
    name: postgres_retention_password
  scoring_service_token:
    external: true
    name: scoring_service_token
  pii_encryption_key:
    external: true
    name: pii_encryption_key
  pii_hmac_key:
    external: true
    name: pii_hmac_key
  terms_runtime_password:
    external: true
    name: terms_runtime_password
  terms_migrator_password:
    external: true
    name: terms_migrator_password
  terms_retention_password:
    external: true
    name: terms_retention_password
  terms_backup_password:
    external: true
    name: terms_backup_password
  terms_internal_service_token:
    external: true
    name: terms_internal_service_token
  terms_fingerprint_key:
    external: true
    name: terms_fingerprint_key
