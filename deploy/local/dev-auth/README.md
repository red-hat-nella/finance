# Emisor JWT local

Este directorio contiene un servidor JWKS solo para el perfil Compose de desarrollo.
Las claves se generan en `deploy/local/.secrets/`, nunca se versionan y no son validas
para OpenShift.
