apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels: {app.kubernetes.io/part-of: alternative-credit-scoring}
data:
  API_BASE_URL: /api/v1
  AUTH_MODE: oidc
  OIDC_ISSUER: https://identity.example.test/realms/credit
  OIDC_CLIENT_ID: alternative-credit-scoring-web
  OIDC_SCOPE: openid profile
