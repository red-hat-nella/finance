apiVersion: v1
kind: Service
metadata:
  name: scoring
  labels: {app.kubernetes.io/name: scoring, app.kubernetes.io/part-of: alternative-credit-scoring}
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: scoring}
  ports: [{name: http, port: 8080, targetPort: http, protocol: TCP}]
