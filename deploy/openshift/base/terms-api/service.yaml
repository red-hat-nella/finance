apiVersion: v1
kind: Service
metadata:
  name: terms-api
  labels: {app.kubernetes.io/name: terms-api, app.kubernetes.io/part-of: alternative-credit-scoring}
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: terms-api}
  ports: [{name: http, port: 8080, targetPort: http, protocol: TCP}]
