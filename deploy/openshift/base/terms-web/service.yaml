apiVersion: v1
kind: Service
metadata:
  name: terms-web
  labels: {app.kubernetes.io/name: terms-web, app.kubernetes.io/part-of: alternative-credit-scoring}
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: terms-web}
  ports: [{name: http, port: 8080, targetPort: http, protocol: TCP}]
