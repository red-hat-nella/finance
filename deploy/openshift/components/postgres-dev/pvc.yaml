apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  labels: {app.kubernetes.io/name: postgres, app.kubernetes.io/part-of: alternative-credit-scoring}
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: gp3
  resources: {requests: {storage: 5Gi}}
